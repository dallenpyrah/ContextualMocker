name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ] # Adjust branch name if needed
  pull_request:
    branches: [ "main", "master" ] # Adjust branch name if needed

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin' # Or another distribution like 'zulu', 'adopt'
        cache: maven

    - name: Build, Test, and Package with Maven
      run: mvn -B verify --file pom.xml

    # Optional: Upload JAR artifact
    # - name: Upload artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: contextualmocker-jar
    #     path: target/*.jar # Adjust path if JAR name is specific 